"""Deterministic extraction of nf-core run outputs into a RunManifest.

This package parses; it assigns no meaning. Metric keys are kept exactly as the
source file spells them — mapping a key onto a NExtSEEK sample attribute is the
mapper's job (see reingest_maps/ and Plan 2).
"""
