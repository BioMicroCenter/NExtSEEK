[
  {
    "endpoint": "/nextseek_api/samples/graph_search/",
    "methods": ["POST"],
    "source": "nextseek_api/graph_search/",
    "rationale": "Search endpoint over the metadata graph; runs in a Neo4j READ transaction and injects the caller's project clause server-side, so it mutates nothing and widens no caller's visibility."
  },
  {
    "endpoint": "/nextseek_api/projects/",
    "methods": ["GET"],
    "source": "min_api_endpoints_enriched.json",
    "rationale": "List projects; read-only GET."
  },
  {
    "endpoint": "/nextseek_api/sample_types/",
    "methods": ["GET"],
    "source": "min_api_endpoints_enriched.json",
    "rationale": "List sample types; read-only GET."
  },
  {
    "endpoint": "/nextseek_api/assays/",
    "methods": ["GET"],
    "source": "min_api_endpoints_enriched.json",
    "rationale": "List assays; read-only GET."
  },
  {
    "endpoint": "/nextseek_api/samples/retrieve/",
    "methods": ["POST"],
    "source": "read_safety_audit_2026-07-06",
    "rationale": "Retrieve/export sample metadata by UID or SEEK ID (incl. parent/child lineage); read-only. POST carries the identifier list, not a mutation. Verified non-mutating in nextseek_api/services/sample_retrieve.py (SELECTs and a read transaction only)."
  },
  {
    "endpoint": "/nextseek_api/investigations/",
    "methods": ["GET"],
    "source": "read_safety_audit_2026-07-06",
    "rationale": "List investigations; read-only GET (sibling of the already-allowlisted samples/projects/assays lists)."
  },
  {
    "endpoint": "/nextseek_api/people/",
    "methods": ["GET"],
    "source": "read_safety_audit_2026-07-06",
    "rationale": "List people; read-only GET (sibling of the already-allowlisted samples/projects/assays lists)."
  },
  {
    "endpoint": "/nextseek_api/sops/",
    "methods": ["GET"],
    "source": "read_safety_audit_2026-07-06",
    "rationale": "List SOPs; read-only GET (sibling of the already-allowlisted samples/projects/assays lists)."
  }
]
