{
  "report_type": "NFCORE_SEQINSPECTOR_SAMPLESHEET",
  "description": "nf-core/seqinspector samplesheet context JSON template, aligned to the pinned 1.1.0 nextflow_schema.json and assets/samplesheet.csv. QC-only pipeline: no genome reference required.",
  "params": {
    "tools_bundle": {
      "type": "enum",
      "allowed": [
        "default",
        "all",
        "minimal"
      ],
      "default": "default",
      "steerable": true,
      "help": "named bundle of QC tools to run"
    },
    "tools": {
      "type": "string",
      "default": null,
      "steerable": true,
      "help": "explicit comma-separated tool list; overrides tools_bundle when set"
    },
    "skip_tools": {
      "type": "string",
      "default": "fastqscreen,picard_collectmultiplemetrics",
      "steerable": true,
      "help": "Two tools from the 'default' bundle are skipped here, both because they need reference data Luria does not carry. fastqscreen's reference list defaults to an EXAMPLE csv pointing at three S3 iGenomes indices. picard_collectmultiplemetrics sets needs_bam in workflows/seqinspector.nf, which routes reads through BWAMEM2_MEM \u2014 and our refs tree has fasta+gtf but no bwamem2 index. What remains (fastqc, fq_lint, seqfu_stats, sequali) is pure read-level QC that needs nothing at all."
    },
    "subsample_tools": {
      "type": "string",
      "default": "fastqscreen,kraken2,picard_collectmultiplemetrics",
      "steerable": true,
      "help": "tools that run on a subsample rather than the full FastQ"
    },
    "sample_size": {
      "type": "number",
      "default": 0,
      "steerable": true,
      "help": "reads to subsample for subsample_tools; 0 means no subsampling"
    },
    "continue_with_lint_fail": {
      "type": "bool",
      "default": true,
      "steerable": true,
      "help": "keep going when fq_lint flags a file (default true \u2014 a lint failure should not abort a QC survey)"
    },
    "fq_lint_args": {
      "type": "string",
      "default": null,
      "steerable": true
    },
    "bbmap_clumpify_args": {
      "type": "string",
      "default": "markduplicates=true",
      "steerable": true
    },
    "save_bbmap_clumpify_reads": {
      "type": "bool",
      "default": false,
      "steerable": true
    },
    "kraken2_db": {
      "type": "string",
      "default": null,
      "steerable": true,
      "help": "REQUIRED if kraken2 is in the tool set. No kraken2 database is provisioned on Luria \u2014 leave kraken2 out of the bundle unless a db path is supplied."
    },
    "multiqc_title": {
      "type": "string",
      "default": null,
      "steerable": true,
      "help": "MultiQC report title"
    },
    "fastq_screen_references": {
      "type": "string",
      "default": null,
      "steerable": true,
      "help": "Curated CSV of screening references (name,dir,basename,aligner). Required if fastqscreen is enabled; the pipeline default is an example file pointing at S3."
    }
  },
  "reference_resources": [],
  "pipeline": {
    "name": "nf-core/seqinspector",
    "repository": "https://github.com/nf-core/seqinspector",
    "documentation_url": "https://nf-co.re/seqinspector/latest/docs/usage/",
    "input_file_type": "csv",
    "core_rule": "Columns are sample, fastq_1, fastq_2, rundir, tags. Only sample and fastq_1 must carry a value; fastq_2 is blank for single-end, and rundir/tags are optional.",
    "required_columns": [
      "sample",
      "fastq_1",
      "fastq_2"
    ],
    "optional_columns": [
      "rundir",
      "tags"
    ],
    "reference_requirement": "NONE. --genome/--fasta are optional; with neither, the pipeline skips the BWAMEM2 alignment step and every tool that depends on the alignment. This is the intended mode here: the Luria refs tree carries only fasta+gtf and no bwamem2 index, so requesting alignment would force an expensive per-run index build.",
    "multi_run_behavior": "Rows sharing a sample value are treated as multiple runs/lanes of the same sample.",
    "single_vs_paired_behavior": "Paired-end when fastq_2 is populated; single-end when blank."
  },
  "samplesheet_template": [
    {
      "sample": null,
      "fastq_1": null,
      "fastq_2": null,
      "rundir": null,
      "tags": null
    }
  ],
  "examples": {
    "paired_end_example": {
      "sample": "AEG588A1",
      "fastq_1": "/net/bmc-pub10/data1/bmc/fastq/AEG588A1_S1_L002_R1_001.fastq.gz",
      "fastq_2": "/net/bmc-pub10/data1/bmc/fastq/AEG588A1_S1_L002_R2_001.fastq.gz",
      "rundir": null,
      "tags": "cohort_control:lane1"
    },
    "single_end_example": {
      "sample": "AEG588A2",
      "fastq_1": "/net/bmc-pub10/data1/bmc/fastq/AEG588A2_S2_L002_R1_001.fastq.gz",
      "fastq_2": "",
      "rundir": null,
      "tags": "cohort_treated"
    }
  },
  "validation_rules": [
    "The file consumed by the pipeline must be comma-separated (CSV).",
    "sample must be non-empty; spaces are converted to underscores by the pipeline.",
    "fastq_1 must be a gzipped FASTQ path ending in .fastq.gz or .fq.gz.",
    "fastq_2 must be blank for single-end libraries and populated for paired-end libraries; the header must exist either way.",
    "tags is a colon-separated list of group labels; every distinct tag produces its own MultiQC report in addition to the global one.",
    "Do NOT add kraken2 to the tool set without also setting kraken2_db \u2014 the run will fail with no database.",
    "Do NOT pass --gtf: seqinspector's schema has no gtf parameter and nf-schema aborts the run on an unrecognised param.",
    "The 'default' tool bundle is fastqc, fastqscreen, fq_lint, picard_collectmultiplemetrics, rundirparser, seqfu_stats, sequali \u2014 it does NOT include kraken2, and the pipeline error()s if kraken2 is requested without kraken2_db, so there is no silent database download. fastqscreen is the one to watch: skipped by curated default because its reference list is an example pointing at S3.",
    "picard_collectmultiplemetrics is in the 'default' bundle and requires an ALIGNMENT: workflows/seqinspector.nf sets `needs_bam` from it and feeds BWAMEM2_MEM. Whether that silently skips without a genome or builds an index was never established, so it is skipped rather than relied upon. Re-enable only once a bwamem2 index is staged."
  ],
  "notes": "QC-only. Nothing here writes analysis results worth reingesting as A.* samples \u2014 the output is MultiQC reports for human review. Use it to triage a cohort before committing to a full pipeline, or to audit sequencing data already registered in NExtSEEK.",
  "sources": [
    {
      "label": "nf-core/seqinspector usage docs (latest)",
      "url": "https://nf-co.re/seqinspector/latest/docs/usage/",
      "notes": "Samplesheet columns, optional-genome behaviour, default tool set."
    },
    {
      "label": "nf-core/seqinspector nextflow_schema.json @ 1.1.0",
      "url": "https://raw.githubusercontent.com/nf-core/seqinspector/1.1.0/nextflow_schema.json",
      "notes": "Authoritative param names, types and defaults for the pinned revision; confirms no gtf param."
    },
    {
      "label": "nf-core/seqinspector assets/samplesheet.csv",
      "url": "https://github.com/nf-core/seqinspector/blob/master/assets/samplesheet.csv",
      "notes": "Canonical header: sample,fastq_1,fastq_2,rundir,tags."
    }
  ],
  "schema": {
    "sections": {
      "samplesheet_columns": {
        "description": "Column-level field dictionary for the nf-core/seqinspector input samplesheet.",
        "fields": {
          "sample": {
            "required": "required",
            "position_rule": "Must be column 1",
            "expected_value": "string",
            "allowed_values": "",
            "description": "Custom sample name. Repeat across rows for multiple lanes/runs of the same library.",
            "validation_hints": "Prefer the NExtSEEK D.SEQ UID so the QC report maps back to the sample record.",
            "example": "D.SEQ-260711ENG-12-PUB"
          },
          "fastq_1": {
            "required": "required",
            "position_rule": "Must be column 2",
            "expected_value": "path string",
            "allowed_values": ".fastq.gz or .fq.gz",
            "description": "Full path to read 1 FASTQ.",
            "validation_hints": "Absolute Luria path; taken from the sample's Link_PrimaryData / File_PrimaryData metadata.",
            "example": "/net/bmc-pub10/data1/bmc/fastq/sample_R1.fastq.gz"
          },
          "fastq_2": {
            "required": "required header, value optional for single-end",
            "position_rule": "Must be column 3",
            "expected_value": "path string or blank",
            "allowed_values": ".fastq.gz or .fq.gz, or blank",
            "description": "Full path to read 2 FASTQ for paired-end libraries.",
            "validation_hints": "Leave blank for single-end; keep the header.",
            "example": "/net/bmc-pub10/data1/bmc/fastq/sample_R2.fastq.gz"
          },
          "rundir": {
            "required": "optional",
            "position_rule": "After the required columns",
            "expected_value": "path string",
            "allowed_values": "",
            "description": "Sequencing run folder, used by rundirparser to collect instrument metadata.",
            "validation_hints": "Populate only when the run directory is known and readable on Luria; a wrong path fails the rundirparser tool.",
            "example": "/net/bmc-pub10/data1/bmc/runs/260711_A00123"
          },
          "tags": {
            "required": "optional",
            "position_rule": "After the required columns",
            "expected_value": "colon-separated string",
            "allowed_values": "",
            "description": "Grouping labels. Each distinct tag yields its own MultiQC report alongside the global one.",
            "validation_hints": "This is the column worth populating from NExtSEEK metadata \u2014 cohort, treatment, genotype or study give per-group QC reports for free. Use short slugs; avoid commas.",
            "example": "cohort_NDMA:genotype_RaDR"
          }
        }
      }
    }
  },
  "report_writer_guidance": {
    "purpose": "Construct an nf-core/seqinspector samplesheet from a resolved NExtSEEK D.SEQ cohort.",
    "workflow_role": "Selected when the user asks to QC, inspect, survey or triage sequencing data rather than analyse it.",
    "output_expectation": "Emit a pipeline-ready CSV with header sample,fastq_1,fastq_2,rundir,tags.",
    "population_priority": [
      "One row per D.SEQ leaf sample; use the sample's UID as the sample value.",
      "Map R1/R2 FASTQ paths from the sample's primary-data metadata; leave fastq_2 blank for single-end.",
      "Leave rundir blank unless the run directory is genuinely known.",
      "Populate tags from the cohort-defining metadata the user asked about (treatment, genotype, study, cohort) so the run yields per-group MultiQC reports. This is the main reason to run this from NExtSEEK rather than by hand.",
      "Do not set a genome; the QC tools that matter here need none."
    ],
    "final_checks": [
      "Header is exactly: sample,fastq_1,fastq_2,rundir,tags.",
      "Every row has a non-empty sample and fastq_1.",
      "No --gtf, and no genome unless the user explicitly asked for the alignment-dependent tools.",
      "kraken2 is absent from the tool set unless a kraken2_db path was supplied."
    ]
  }
}
