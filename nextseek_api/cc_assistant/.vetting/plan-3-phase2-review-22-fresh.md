# Phase-2 fresh re-vet (iter 22) — TARGET: PLAN-3-ui-based-io.md

Cold-context independent review. Authority: SPEC-3 (locked E1–E10, §6.2 enriched) > PLAN-3 > task specs.
Verified the plan's reuse/anchor claims against live source under `nextseek_api/cc_assistant/`, `nextseek_api/services/`, `nextseek_api/assistant/`, `nextseek_api/batch_upload/`, `chat_frontend/`, and PLAN-7's start-gate.

## 2A — Vet (will it execute without a hitch; permissions/paths/snags)

- **MEDIUM — Task 9b `upload_list` references `ProjectResolutionError` it does not import; the module-top import only lands in Task 10 (a *later* task).**
  Location: Task 9b Step 2 paste —
  ```python
  from nextseek_api.cc_assistant.cc_provision import resolve_user_project, build_user_dirs
  ...
  except ProjectResolutionError:
  ```
  Verified live: `services/cc_assistant.py` imports `Response` at module top (line 27, OK) but **does NOT import `ProjectResolutionError` at module top** — it is imported only *locally inside `_run`* (lines 242–246). The module-top `from …cc_provision import ProjectResolutionError` is added by **Task 10 Step 1**, which executes *after* Task 9b. So the Task 9b paste, copied verbatim, raises `NameError: name 'ProjectResolutionError' is not defined` at call time. No hermetic test exercises the action body (only the pure `list_input_files` helper is tested), so nothing catches it until the paid Task 13 live gate. The "Task execution order (load-bearing)" constraint flags only 11a→11; this 9b→10 import coupling is unflagged.
  Why a defect: a paste-ready block that NameErrors, in a plan whose whole style is self-contained paste-ready blocks; latent and invisible to the hermetic suite.
  Fix: make Task 9b self-contained — change its local import to `from nextseek_api.cc_assistant.cc_provision import resolve_user_project, build_user_dirs, ProjectResolutionError` (mirroring Task 9's `upload` action, which *does* import it locally). Optionally add a one-line note in the load-bearing-order constraint.

- Permissions/paths catalogued and confirmed present: `dirs.input_mnt` (Task 3 add), `dirs.output_mnt`/`output_src`/`scratch_src`/`cc_state_mnt`/`scratch_mnt` (all exist on `UserDirs`); `CCPaths(host_user_root=, user_root_mount=)` + `from_env()` match Task 3 test; `build_user_dirs(paths, project_dirname, user_id, *, session_id)` positional signature matches Task 3 test; `_validate_segment` regex `^[A-Za-z0-9._@+-]{1,128}$` admits `"42-px"`/`"alice"` (Task 3 test builds clean); `register_job(user_id, job_id, project_id, …)` and `user_owns_job(user_id, job_id, …)` signatures match Task 9 calls; `BATCH_UPLOAD_MAX_TOTAL_BYTES` consumed via `getattr(settings, …, 200MB)` exactly as Task 9; `celery_app.py` has `app = Celery("batch_upload")` + `import …cc_sweep` anchor for Task 9 Step 3b; `ProjectIdentity` exposes `.id/.slug/.dirname` used by upload/list actions. No snag.

## 2B — Stress Test

- **Most likely failure mode:** the Task 9b NameError above (masked until live), and/or persistence wiring left stubbed. The plan closes the latter with two mutation-sensitive source guards (Task 11 Step 4: `on_turn_complete(TurnCompletePayload(` in `cc_engine.py`; `on_turn_complete=_append_cc_turn_complete` in `services/cc_assistant.py`) plus the Task 13 reload assertion. Adequate.
- **Most catastrophic:** cross-user artifact/transcript leak. Mitigated: every read endpoint is `ChatSession.objects.filter(user=request.user)` owner-scoped; `resolve_artifact_path` uses `_safe_relpath` + `is_relative_to(root)`; decompress is bomb-bounded (Task 1, tested). 1c-memory regression (Task 4 `classify_tool_use` refactor) — I checked the refactor byte-for-byte against the live `_tool_use_line` (`cc_summary.py:87`): bash/write/edit/multiedit/notebookedit/read/skill/task/other all render identically (`detail or ''`, skill `detail or name` fallback). No regression; full-suite gate in Task 4 Step 4 guards it.
- **Hidden dependencies:** verified `/data/scratch` (`_CONTAINER_SCRATCH`) binds to `dirs.scratch_src` (user scratch **root**, not a per-run subdir), so agent writes to `scratch/raw/...` yield diff relpaths `raw/...` — `partition_changed`'s `startswith("raw/")` classifies correctly. The persist block's `Path(dirs.cc_state_mnt)/ "projects"` is safe because `_run` always sets `cc_state_key = str(chat_session.session_id)` (line 223) before calling `run_cc_turn(cc_state_key=…)`, so `cc_state_mnt` is never `None` on the CC path. No hidden gap.
- **Ambiguous success conditions:** Task 13 Step 6 correctly distinguishes the *live* `data["cc_traces"]` (attached pre-persist, always rides out) from the *post-reload* `GET …?include=turns` jq excerpt that reads the DB `chat_log` — so a silent DB persist failure is caught by the reload assertion, not masked by the live payload. Falsifiable and non-gameable.
- **Coverage risk:** the ≥95% floors are on genuinely pure modules (transcript_store, trace, artifacts, provision, upload_validate, upload_list, models_api) and are reachable (I traced cc_trace's branches against the fixture — created/modified/none `action` paths, ok/error `status`, `_Other` fallthrough, tools tally all exercised; line coverage, not branch, so the no-id/no-message edges don't sink the floor). `cc_upload_validate` 100% via the justified `# pragma: no cover` on the provably-unreachable belt-and-suspenders line — legitimate. Task 5 (translate) coverage **exception** is legitimate: a pre-existing shared *procedural* module, surgical two-key change, uncovered lines (58/68/97/104/123) provably outside `_handle_result`; paired with two hermetic assertions + the non-deferrable Task 13 live gate. Tasks 11/11a/12/13 integrated behavior has no hermetic seam by documented box constraint (no test-DB CREATE, no in-suite live HTTP) — paired with the non-deferrable, committed-evidence Task 13 live gate that also gates PLAN-7 Step 7. All exceptions legitimate and gated.
- **Rollback:** `:pre-step3` snapshot + `rollback.sh`; best-effort persist guarantees a paid reply is never converted to `query_error` (no spend-doubling) — risk-register row 9 owns this. Sound.

## 2C — Validate External Dependencies

- `zstandard>=0.25` — `ZstdCompressor.compress` / `ZstdDecompressor.stream_reader` bounded read used in Task 1 are real, stable APIs; added in Task 1 (not deferred), `--with zstandard` in the harness. OK.
- pydantic v2 unpinned — plan uses ordered `Union[_Assistant,_User,_Other]` with `_Other` last (no discriminated-union pin), `TypeAdapter.validate_python`, `model_validate`/`model_dump`. Matches existing repo usage (`cc_summary.py:186`). `_Other.type: str | None` intentionally tolerates `parse_transcript`'s `{"_type":"unparsed"}` lines — correct against the real jsonl shape (deviates from SPEC §6.3's `type: str`, but the plan documents this and the real-shape choice is right; not a defect).
- orjson — already in 1c. OK.
- Frontend (verified live): `chat_frontend/package.json` has `"test": "vitest run"` and `"build:embedded": "tsc -b --noEmit && vite build --config vite.config.embedded.ts"`; `vite.config.embedded.ts` `outDir = ../static/js/chat_assistant` and `base = /static/js/chat_assistant/`. Task 12 Step 6 / Task 13 Step 2 claims are accurate; SPEC §13's `[CONFIRM@PLAN]` build-path is correctly resolved.
- PLAN-7 cross-target handshake (verified): PLAN-7's start-gate validator (PLAN-7 §… line 137) greps the **byte-identical** allowlist named by PLAN-3 Task 13 Step 8 — (1) `Applying nextseek_api\.0007` OR `[X] 0007_ccsessiontranscript` (the latter guaranteed by PLAN-3's added `showmigrations nextseek_api` capture → idempotency-robust), (2) `cc_assistant.upload`, (3) `cc_traces`. Both sides explicitly disclaim grepping command substrings. Handshake is consistent and producible by Task 13's commands. OK.

## 2D — Gameproof

- Success conditions are concrete and re-runnable per task (named pytest commands with `--cov-fail-under=95`, named live assertions). The historically gameable ones are closed:
  - "marked DONE, wiring stubbed" → the two Task 11 Step 4 source guards (verified the target strings will exist in the pasted code) + Task 13 reload.
  - "Dropbox string moved to a comment" → Task 8 grep-guard scans whole `cc_assistant/` + `services/cc_assistant.py`; also asserts `/srv/dmac/users` present and `/Users/taishajoseph` absent (verified the laptop path is still live at `cc_config.py:15`).
  - "overfit extractor to one fixture" → Task 4 Step 9b second fixture (`cc_transcript_multitool.jsonl`) with different tools.
  - "agent prose in markdown counts as live proof" → Task 13 requires committed `live_gate_transcript.txt` whose markers PLAN-7's hard start-gate independently greps; PLAN-7 Step 7 cannot begin without it.
  - Projection passthrough drop → Task 7 `test_projection_passes_cc_traces_through` source guard; substring `cc_traces=entry.get("cc_traces")` is a real substring of the Step 4 paste `cc_traces=entry.get("cc_traces") or None` (verified) — guard stays meaningful.
  - FIFO-cap / E5-mirror mutation → `append_capped` keep-newest test + `apply_turn_to_extra_state` dual-store mutation test (both pure, Django-free). The E5 mirror (SPEC-3 §6.5/E5) is restored and guarded — matches locked design.
- **LOW — Task 6 Step 5b under-specifies the rewrite of the two *existing* `test_cc_engine_publish.py` functions.** The reworked `_publish_artifacts` makes `turn_id` a **required keyword-only** arg (no default) and changes the return to a dict and the on-disk path to `output/artifacts/<turn_id>/<rel>`. Both existing tests (`test_publish_artifacts_copies_nested_scratch_changes`, `test_publish_artifacts_skips_symlinks`) call it *without* `turn_id` and assert the old `list[str]` shape / old `output/<rel>` path, so both `TypeError`/assert-fail. The plan says "Update … dict keys + >1 file emits single zip" and gives the coverage command, but — unlike the rest of the plan — does not paste the full replacement nor explicitly enumerate that *both* functions need `turn_id=` + the new path + dict assertions. A careful implementer can infer it; flagged only because the plan's paste-ready standard sets a higher bar here.
  Fix: paste the two updated test bodies (or explicitly list: add `turn_id="run1"`, assert `(output/"artifacts"/"run1"/…)`, assert returned `dict` keys).

## Non-blocking cosmetic notes
- `run_cc_turn` docstring still says "scoped Dropbox mounts" (`cc_engine.py:417-420`); Task 6 Step 6's "Docstring hygiene" note already schedules this fix.
- Plan prose occasionally cites approximate line anchors ("`:580-587`", "lines ~181–212"); all the ones I spot-checked (`_publish_artifacts` region 573–588, `_handle_result` 130–156, projection 505–529, MessageBubble 77–79/106/111) matched the live source, so the approximate anchors are reliable.
