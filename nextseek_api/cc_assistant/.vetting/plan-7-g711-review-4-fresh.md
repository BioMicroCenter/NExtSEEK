# Fresh adversarial review — G7-11 amendment + sidecar wave, iteration 4 (post iter-3 hardening)

**Reviewer:** fresh cold-context adversarial reviewer (iteration 4 of the sidecar-wave Phase 2 loop; did not author the amendment, did not conduct iterations 1–3)
**Artifact:** commit `55aa503` on worktree `cc-step7-compose-native` — SPEC-7 G7-11 sections (§2/§3/§4/§8/§10/§11/§12/Amendment Log) + PLAN-7 sidecar wave preamble and Tasks 12–16 + the three amended locked-text spots (Task 2 Step 2 conditional Compose-floor sentence, Task 7 Step 2 env keys, Task 8 Step 1 doc guard)
**Grounding independently re-verified against:** branch code — `cc_engine.py` (`build_agent_environment` full signature and emitted env :220-266, `_TIMEOUT_HARD_MAX = 180` :70-73, `_run_kwargs` labels-only :334-373, `_rewrite_loopback_url` :201-217), `tests/validate_step7_compose_deploy.py` (`check_agent_env_decredentialed` + `_CC_SHARED_CRED_KEYS`/leak-marker oracle :813-822; `SHARED_CRED_KEYS` import :38), `tests/test_cc_realstack.py` (harness imports `cc_engine` directly :39 — the builder is callable from the gate harness today), `docker/nginx.conf` (grep-verified: **no** `access_log` directive anywhere), `docker-compose.yml` (nginx.conf ro-mounted :39); NExtSEEK endpoints — `nextseek_api/assistant/granular.py` (`_graph` :72-89: cypher-present path executes and can return successful-empty; no-cypher path returns the `ok: False` dict the L-5 pin now rejects); pinned upstream `/home/taishajo/work/dmac-assistant` @ `a429f13` — `_sidecar_client.py:63-64` (`NEXTSEEK_SIDECAR_HOST`/`PORT` defaults), `_nextseek_runner.py:105-106, 245-249` (`NEXTSEEK_URL` REQUIRED, `API_USER`/`API_PASS`), `_assistant_client.py:110` (query + plan both POST `query/async/`), `nextseek-query`/`nextseek-plan` bins (both route through `_nextseek_runner` → `_assistant_client`); `.vetting/defect-lineage.md` G7-11 threads. Neither prior reviews nor the hardener were trusted on any load-bearing claim.
**Date:** 2026-07-02

## Verdict: **UNCONDITIONAL_ACCEPTANCE** — 0 CRITICAL / 0 HIGH / 0 MEDIUM / 6 LOW

The iter-3 hardening is real and complete on all three re-raised M threads. The executor-env fix is not just asserted — I verified the mechanism it mandates is actually buildable: `build_agent_environment` is a pure keyword-only function (`source`, `api_user`, `api_pass`, `path_mappings`) with no session/turn state, importable by the realstack harness (which already imports `cc_engine`), and the env it emits covers every input the 9 bin ops read. The wall_secs evaluation is a single well-defined pinned constant with a stated derivation and a loud, named-user-decision disposition. The M-3 branch is struck in both governing texts with no exploitable residue. Threads G and H close. What remains is six LOW documentation-lag/refinement nits, all fail-closed or advisory — none holds a thread under this loop's own precedent (iter-3: "L-5 is a documentation-tightening nit, not a thread-holder").

---

## Adjudication of the iter-3 hardening items

### 1. M-1 fix — executor env via the production `build_agent_environment` code path + `matrix_env_scan.txt` — **SOUND; thread H leg closed**

Pressure-tested against `cc_engine.py`, not taken on faith:

- **Callable outside a turn?** Yes. `build_agent_environment(*, source=None, api_user, api_pass, path_mappings)` (`cc_engine.py:220-226`) takes no session, no user object, no container handle — only the gate user's own credentials (which the gate legitimately holds; it authenticates REST calls with them for `seeded_fixture.json` already), an optional topology `source` mapping, and a JSON-serializable `path_mappings`. The harness file already does `from nextseek_api.cc_assistant import cc_config, cc_engine` (`test_cc_realstack.py:39`), so "calls the engine's own builder" is a one-line reality, and the docstring's own contract ("SINGLE source of truth for the agent env — a secret can never sneak in via a separate inline dict") is exactly the guard-inheritance the fix buys.
- **Does the emitted env suffice for the 9 ops?** Yes. The two viewset ops (`nextseek-query`/`nextseek-plan`) run `_nextseek_runner` → `_assistant_client`, which requires `NEXTSEEK_URL` (`_nextseek_runner.py:105` — hard `os.environ[...]`), `API_USER`/`API_PASS` (:245-249) — all emitted by the builder (:250-263, with the loopback→`nextseek_nginx` rewrite correct for a `dmac-cc-net` executor). The 7 sidecar ops additionally read `NEXTSEEK_SIDECAR_HOST`/`PORT` (`_sidecar_client.py:63-64`, defaults `nextseek-sidecar`/`8765`), which Task 13 Step 2 adds to this same builder before Task 15 runs — and whose defaults match the compose service name pinned by Task 13 Step 1 even if the keys were absent. No op reads anything the builder cannot provide.
- **Machine check:** `matrix_env_scan.txt` is REQUIRED in both texts (PLAN-7:1034-1038 "REQUIRED companion artifacts"; SPEC-7 §8:250-253 "proven by"), validated "with the SAME no-shared-creds rules as `agent_env_scan.txt`" — trivially implementable: `check_agent_env_decredentialed` (`validate_step7_compose_deploy.py:813-822`) is a filename-parameterizable pattern over the imported `_CC_SHARED_CRED_KEYS` + leak markers, and Task 15's Success list explicitly rejects "a matrix_env_scan carrying shared creds" and a bundle missing it (PLAN-7:1080-1083). Executor teardown is now pinned ("harness controls executor lifetime and removes it after", PLAN-7:1032) — the iter-3 parked-credentialed-container residue is addressed.

Residues are LOW only: the builder's `source` mapping for the harness is unpinned (L-3) — fail-closed if wrong (missing `NEXTSEEK_URL` → runner ops crash → gate red, never green).

### 2. M-2 fix — per-op `in_turn_viable` = `wall_secs` < pinned 150 s — **SOUND; thread G leg closed**

- **Well-defined:** a single constant, one comparison, in both texts (PLAN-7:1039-1045; SPEC-7 §8:253-257). No competing number anywhere (grep-verified: 150 s appears only in these two spots + ledger rows). Verdict semantics, bundle effect (stays green), and disposition (MUST appear in `validator_output.txt` + Task 11 handoff as a named user decision with three enumerated options) are all pinned. `wall_secs` is thereby genuinely evaluated — iter-3 M-2's core demand — and the fix is stricter than the ask (iter-3's fix direction accepted flagging at ≥180; the hardener pinned 150).
- **Derivation:** stated — "150 s = `_TIMEOUT_HARD_MAX` − boot/prompt slack" — and the 180 base is verified real (`cc_engine.py:70-73`, env-clamped by `min(...)`). The 30 s slack figure is an engineering allowance, not a measured constant, and the gate's `wall_secs` (bare op execution) excludes the in-turn LLM round-trip time a production turn also spends — so "viable" is an optimistic upper bound (L-2). That does not break honesty: the mechanism is a surfacing threshold feeding a user decision, not a certification, and the enumerated decision options ("raise the cap? … async op pattern?") put latency squarely in front of the user.
- **"Capability ≠ latency" holds** at this loop's bar: the failure mode the G7-11 ruling exists to kill is the *silent* facade (op certified, backend absent or unusable, nobody told). Exceeders here are machine-listed and MUST become a named user decision before Task 11's tracker update — loud, attributed, and user-owned. No silent deferral remains.

### 3. M-3 fix — every row `Name == dmac-cc-matrix-<run_id>`, no exceptions — **SOUND; thread H leg closed**

The parenthetical is struck: PLAN-7 Task 15 Step 3 now requires the executor name "for EVERY row … no exceptions" (:1050-1054) and SPEC-7 §8 states "no in-turn-agent exception" (:253-254). Grep for the branch's residue (`agent name if` / `exec'd in-turn`) finds only the strike-notice itself. The fabrication path iter-3 identified (stamp rows with the real agent's container_id from the during-turn inspect, skip the executor) is closed — the join must land on the executor name in the matrix-window inspect for all 9 rows. One PLAN/SPEC inconsistency survives, but it is *older* text the fix did not touch and it fails closed: Task 15 Step 1's parenthetical still says the executor-attachment check joins "via `network_inspect.json`" (:983-985), i.e. the during-turn capture that structurally cannot contain the executor — Step 3 and §8 unambiguously name `network_inspect_matrix.json` (L-1, same textual-lag class as iter-3's L-2 in the adjacent sentence).

### 4. L-fix set (iter-3 L-1..L-5) — **all five verified in place**

- **L-1 (§8 parenthetical):** "(the in-turn sweep's output …)" replaced by "(the post-matrix trusted-sweep invocation's output, recorded in `sweep_invocation.json` …)" (SPEC-7 §8:248-250). Closed.
- **L-2 (Step 1 schema):** `container_name` added to Task 15 Step 1's per-op record (PLAN-7:981-983), matching §8:236. Closed.
- **L-3 (`sweep_invocation.json`):** pinned with the exact 4-field shape `{command, exit_code, output_excerpt, timestamp}` in both texts (PLAN-7:1036-1038; SPEC-7 §8:249-250), presence + exit-0 enforcement in PLAN's Step 3 and Success list. Closed. (Task 14's narrative sentence :957-958 still says "recorded in evidence (command, exit code, output excerpt)" without the artifact name or timestamp — lagging subset, not a contradiction; folded into L-5 below.)
- **L-4 (nginx `access_log` + query/plan shared endpoint):** `docker/nginx.conf` is grep-verified to contain no `access_log` directive today (the artifact currently rides the `nginx:latest` compiled default), and Task 13's Files list now owns adding the explicit directive + a hermetic guard test (PLAN-7:852-854); Task 15 Step 1 carries the endpoint-mapping note (:1001-1007) and Success adds "(query/plan share one endpoint check)" (:1083-1084). The shared-endpoint claim is independently re-verified: both bins route `_nextseek_runner` → `_assistant_client.run_query` → `POST query/async/` (`_assistant_client.py:110`; `_nextseek_runner.py:97-137, 233-240` — `--planner` only flips `mode`, not the path). SPEC-7 §8's "≥1 hit per op endpoint" formulation degenerates naturally to the same check — consistent. Closed, with one placement nit (L-4 below: the requirement lives only in a Files-list parenthetical; no Task 13 step or success condition carries it).
- **L-5 (successful-empty only):** PLAN-7:1060-1067 now pins "exit 0 with a documented SUCCESSFUL-empty excerpt shape … an exit-0 excerpt carrying a failure payload such as `{"ok": false, "error": "graph agent produced no cypher"}` MUST FAIL". Closed — and I specifically checked this tightening for thread-I damage (below): it does not make the gate unachievable.

---

## Thread verdicts (defect-lineage ledger, G7-11 threads)

- **Thread G — CLOSED.** Parent chain H-1 → R2-H1 → iter-3 M-2 fully resolved: production same-turn sweep invariant hermetically enforced at the real `run_cc_turn` publish hook (unchanged from iter-2/3 verification); single trusted sweep entrypoint shared by production and gate; no cap relaxation; and the last leg — the gate/production fidelity gap — is now machine-evaluated per op against a single pinned 150 s constant with a loud named-user-decision disposition. `wall_secs` is evaluated, not just recorded. Residual L-2/L-6 are refinement nits, not thread-holders.
- **Thread H — CLOSED.** Parent chain H-2/M-4 → R2-M1/M2/M4 → iter-3 M-1/M-3 fully resolved: closed-set name enforcement + dual full inspects + id→Name join (verified iter-3, undisturbed); executor env now produced by the production builder — verified callable by the harness outside a turn and sufficient for all 9 ops — and machine-checked by REQUIRED `matrix_env_scan.txt` under the shipped `_CC_SHARED_CRED_KEYS` oracle; the in-turn-agent join branch is struck in both texts, closing the row-stamping forgery path. Residual L-1 (stale Step-1 join parenthetical) is fail-closed textual lag, same class this loop already rated LOW at iter-3.
- **Thread I — NOT REOPENED (damage check performed).** The iter-3 L-5 tightening touches thread I's surface (the empty-data hatch), so I re-checked achievability under the stricter rule: `granular.py:72-89` — when the graph agent produces cypher, `_graph` executes it (`:85-86`) and a zero-row result is a successful-empty shape; only the no-cypher case yields the now-rejected `ok: False` dict (`:88`), and that case is an agent/LLM failure, not empty data — rejecting it is exactly right, and the fixture-first remedy plus the pinned GCP/Bedrock prerequisites make the successful path reachable. The hatch remains achievable per-op; the tightening strengthens, not damages.
- **Thread J — NOT REOPENED.** The iter-3 delta (`git show 55aa503`) touches none of the three locked-text spots; all three re-verified intact in place: Task 2 Step 2's conditional Compose-floor sentence with the G7-11 self-detection clause, Task 7 Step 2's sidecar keys + server-side LLM prerequisite key names, Task 8 Step 1's guard pinned to manual Phase A/B command patterns (not the bare token "sidecar"). The tracker-7b obligation stands in the wave preamble (:807-811).

---

## LOW findings

- **L-1 (PLAN-7:983-985):** Task 15 Step 1's executor-attachment parenthetical still says "(via `network_inspect.json` join on container id/name)" — under the iter-2 dual-capture respec that file is the DURING-TURN inspect, which structurally cannot contain the executor; Step 3 (:1049-1054) and SPEC-7 §8:236-239 correctly name `network_inspect_matrix.json`. Fail-closed (a literal Step-1 implementation reds every honest bundle), but it is a governing-text contradiction the iter-3 hardener edited around (same sentence got `container_name`) and missed. Fix: two-word edit to `network_inspect_matrix.json`.
- **L-2 (PLAN-7:1041-1042 / SPEC-7 §8:254-255):** the 150 s constant's derivation allocates a flat 30 s "boot/prompt slack" with no stated basis, and gate `wall_secs` measures bare op execution while a production turn also spends its 180 s on LLM round-trips — so `in_turn_viable` is an optimistic upper bound (a 140 s op passes the check yet realistically cannot fit a real turn). Acceptable for a surfacing threshold feeding a user decision, but say so: one sentence noting the constant is an upper bound and the named-decision text should present per-op `wall_secs`, not just the exceeder list.
- **L-3 (PLAN-7:1028-1031):** the harness's `source` argument to `build_agent_environment` is unpinned. The builder reads `NEXTSEEK_BASE_URL`/`DMAC_BEDROCK_PROXY_URL` from `source` (default `os.environ` — the *harness host* env, not the Django container env), and `_nextseek_runner.py:105` hard-requires the resulting `NEXTSEEK_URL`. Fail-closed if wrong (ops crash, gate red), but pin it: harness supplies the nextseek container's env (or an explicit topology mapping) as `source`, recorded in evidence.
- **L-4 (PLAN-7:852-854):** the explicit nginx `access_log` directive + hermetic guard test live only in Task 13's **Files**-list parenthetical; no Task 13 step checkbox or success condition carries them, so a step-driven TDD implementation can complete every checkbox without touching `docker/nginx.conf`. Add one line to Task 13 Step 1 (or Success).
- **L-5 (SPEC-7 §8:249-253; PLAN-7:957-958):** §8 introduces `matrix_env_scan.txt`/`sweep_invocation.json` via "proven by"/"recorded in" without the explicit "REQUIRED" marker it gives `gate_access_log_window.txt`/`post_sweep_user_tree_scan.txt` (:239-243), and presence + exit-0 enforcement is stated only in PLAN Task 15; Task 14's invocation-record sentence still enumerates 3 fields with no artifact name. Align the spec-of-record wording (mark both REQUIRED in §8; have Task 14 name `sweep_invocation.json`).
- **L-6 (PLAN-7:643-655):** Task 11's own text carries no hook for the M-2 named user decision — the obligation lives only in Task 15 Step 3 and SPEC-7 §8, unlike the tracker-7b obligation the loop deliberately recorded in the wave preamble (R2-L1 pattern) precisely so it survives to Task 11 execution time. Mirror that: one preamble or Task 11 Step 2 line ("handoff MUST surface validator-listed `in_turn_viable` exceeders as a named user decision").

## Hardener claims re-verified TRUE (no finding)

- `build_agent_environment` signature/emissions exactly as the fix requires; harness already imports `cc_engine`; docstring pins it as the single env source of truth (audit B3) — guard-inheritance by construction is real.
- `_CC_SHARED_CRED_KEYS` oracle is imported shared state (`validate_step7_compose_deploy.py:38`), so "same no-shared-creds validation" for the matrix scan is a mechanical reuse, not new invention.
- `docker/nginx.conf` genuinely lacks any `access_log` directive; the conf is ro-mounted over `/etc/nginx/nginx.conf` (compose :39), so the compiled-default dependency claim is accurate.
- query/plan shared endpoint verified end-to-end in the pinned upstream bins; `--planner` changes only `mode` in the same `POST query/async/` body.
- 180 s cap and its env clamp unchanged on the branch; no wave text relaxes it.

## Strengths

The iter-3 hardening is the first iteration of this wave where every re-raised item was fixed by *strengthening the machine-checked surface* rather than by prose: the executor env moved from an asserted contract to the production builder plus a scanned artifact under the already-shipped oracle, the fidelity gap moved from unexamined to a pinned computed verdict with a named decision path, and the one genuinely dangerous parenthetical was struck in both texts rather than qualified. All five L-fixes landed where they belong, and the tightened empty-shape rule survives an independent achievability walk of the real op handler.

---

*Per Phase-2 canon: threads G and H CLOSED by this fresh review; I and J remain CLOSED (no damage from the iter-3 delta). 0 C/H/M ⇒ UNCONDITIONAL_ACCEPTANCE; the six LOWs are at the hardener's discretion and none blocks lock. Ledger update: mark G and H CLOSED with this review as the closing evidence.*
