[project]
name = "NExtSEEK"
version = "1.3.0"
description = "Extending SEEK for active management of scientific metadata"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "annotated-types",
  "asgiref",
  "attrs",
  "beautifulsoup4",
  "bleach",
  "certifi",
  "celery",
  "chardet",
  "charset-normalizer",
  "deepmerge",
  "dj-rest-auth",
  # Pinned to the 5.2 LTS line: Django 6.0 dropped MariaDB 10.5 support
  # (requires 10.6+), but this native server runs MariaDB 10.5.x. 5.2 supports
  # 10.5. Revisit if/when the server's MariaDB is upgraded to >=10.6.
  "Django>=5.2,<6",
  "django-contrib-comments",
  "django-cors-headers",
  "django-cprofile-middleware",
  "django-crontab",
  "django-filebrowser-no-grappelli",
  "django-markdownify",
  "django-realtime",
  "django-rest-framework",
  "django-widget-tweaks",
  "djangorestframework",
  "djangorestframework_simplejwt",
  "drf-spectacular",
  "drf-spectacular-sidecar",
  "duckdb",
  "et_xmlfile",
  "filebrowser-safe",
  "filelock",
  "fpdf",
  "fsspec",
  "future",
  "grappelli-safe",
  "gunicorn",
  "h5py",
  "hf-xet",
  "html5lib",
  "huggingface-hub",
  "idna",
  "importlib_metadata",
  "inflection",
  "isodate",
  "jdcal",
  "Jinja2",
  "joblib",
  "jsonref",
  "jsonschema",
  "jsonschema-specifications",
  "lxml",
  "Markdown",
  "MarkupSafe",
  "Mezzanine",
  "mpmath",
  "mysql-connector-python",
  "mysqlclient",
  "neo4j",
  "networkx",
  "numpy",
  "oauthlib",
  "olefile",
  "openpyxl",
  "orjson",
  "packaging",
  "pandas",
  "parse",
  "pathlib",
  "polars[calamine]",
  "pillow",
  "psutil",
  "pydantic",
  "pydantic_core",
  "PyJWT",
  "PyMySQL",
  "pyparsing",
  #"python-dateutil.post0",
  "python-docx",
  "pytz",
  "PyYAML",
  "rdflib",
  "referencing",
  "reportlab",
  "requests",
  "requests-oauthlib",
  "rpds-py",
  "ruff",
  "scipy",
  "simplejson",
  "six",
  "soupsieve",
  "SPARQLWrapper",
  "sentence-transformers",
  "sqlparse",
  "sqlalchemy",
  "sympy",
  "threadpoolctl",
  "tinycss2",
  "tokenizers",
  "torch",
  "tqdm",
  "typing-inspection",
  "typing_extensions",
  "tzdata",
  "tzlocal",
  "Unidecode",
  "uritemplate",
  "urllib3",
  "webencodings",
  "xlwt",
  "zipp",
  "zstandard>=0.25",
  "chat_nextseek",
  "pytest-django",
  "channels>=4.3.2",
  "daphne>=4.2.1",
  "dmac-assistant",
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
# In-tree first-party packages: install EDITABLE so the venv imports the repo
# source directly (one source of truth), not a divergent site-packages copy.
chat_nextseek = { path = "chat_nextseek", editable = true }
# Additive dmac_assistant integration (router + Container-CC). Vendored in-tree
# as a path dependency, same pattern as chat_nextseek above.
dmac-assistant = { path = "dmac_assistant", editable = true }
# When chat_nextseek goes public, switch back to the pinned git install:
# chat_nextseek = { git = "https://github.com/cdemurjian/chat_nextseek.git", rev = "3948d857783e3d130bfe6a880fe53a9eefeb603a" }
torch = [
  { index = "pytorch-cpu" },
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "dmac.settings"

[dependency-groups]
dev = [
    "coverage",
]
# Note: the startup CLI does NOT use a uv dependency group — it runs in an
# isolated env via `uv run --no-project --with typer --with rich`. This avoids
# pulling in the main project's deps (mysqlclient et al.) when the host lacks
# the C build toolchain. See startup.sh.
